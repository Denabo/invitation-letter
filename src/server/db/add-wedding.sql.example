-- =============================================================================
-- Single invitation setup
-- =============================================================================
-- Invitation content now lives in src/config/config.js, so the database only
-- needs the RSVP table from schema.sql.example.
--
-- Optional smoke test insert:
-- =============================================================================

INSERT INTO rsvp_submissions (
    guest_name,
    attendance,
    with_partner,
    partner_name,
    with_kids,
    children,
    has_car,
    has_free_seats,
    free_seats_count,
    message,
    raw_payload
) VALUES (
    'Тестовый Гость',
    'ATTENDING',
    true,
    'Тестовая Пара',
    true,
    '[{"name":"Ребёнок","age":"7"}]'::jsonb,
    true,
    true,
    2,
    'Тестовая анкета',
    '{"source":"manual-smoke-test"}'::jsonb
);
