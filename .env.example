# Backend-only secret. Do not prefix with VITE_ and do not commit real credentials.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Local backend port.
PORT=3000

# Browser-facing API base URL. For local dev use the Bun/Hono API.
VITE_API_URL=http://localhost:3000
